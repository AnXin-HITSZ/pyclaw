apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "spring-backend.fullname" . }}-config
  labels:
    {{- include "spring-backend.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
