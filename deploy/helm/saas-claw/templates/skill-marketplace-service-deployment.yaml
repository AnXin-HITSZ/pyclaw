{{- $svc := index .Values "skill-marketplace-service" -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-skill-marketplace-service
  labels:
    {{- include "saas-claw.labels" . | nindent 4 }}
    app.kubernetes.io/name: skill-marketplace-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: skill-marketplace-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: skill-marketplace-service
        app.kubernetes.io/part-of: saas-claw
    spec:
      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecret }}
      containers:
        - name: skill-marketplace-service
          image: "{{ $svc.image.repository }}:{{ $svc.image.tag }}"
          imagePullPolicy: {{ $svc.image.pullPolicy }}
          ports:
            - containerPort: 8086
          env:
            - name: JAVA_TOOL_OPTIONS
              value: "-Xmx384m -Xms128m"
          envFrom:
            - configMapRef:
                name: {{ .Release.Name }}-shared-config
            - secretRef:
                name: {{ .Values.global.secretName }}
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8086
            initialDelaySeconds: 120
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8086
            initialDelaySeconds: 60
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: skill-marketplace-service
  labels:
    app.kubernetes.io/name: skill-marketplace-service
spec:
  type: ClusterIP
  ports:
    - port: 8086
      targetPort: 8086
  selector:
    app.kubernetes.io/name: skill-marketplace-service
