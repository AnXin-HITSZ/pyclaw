{{- $svc := index .Values "claw-service" -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-claw-service
  labels:
    {{- include "saas-claw.labels" . | nindent 4 }}
    app.kubernetes.io/name: claw-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: claw-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: claw-service
        app.kubernetes.io/part-of: saas-claw
    spec:
      imagePullSecrets:
        - name: {{ .Values.global.imagePullSecret }}
      containers:
        - name: claw-service
          image: "{{ $svc.image.repository }}:{{ $svc.image.tag }}"
          imagePullPolicy: {{ $svc.image.pullPolicy }}
          ports:
            - containerPort: 8082
          env:
            - name: JAVA_TOOL_OPTIONS
              value: "-Xmx512m -Xms192m"
          envFrom:
            - configMapRef:
                name: {{ .Release.Name }}-shared-config
            - secretRef:
                name: {{ .Values.global.secretName }}
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 300
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8082
            initialDelaySeconds: 120
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 192Mi
            limits:
              cpu: 200m
              memory: 640Mi
---
apiVersion: v1
kind: Service
metadata:
  name: claw-service
  labels:
    app.kubernetes.io/name: claw-service
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
  selector:
    app.kubernetes.io/name: claw-service
