apiVersion: v1
kind: Service
metadata:
  name: {{ include "claw-saas-mysql.fullname" . }}
  labels:
    {{- include "claw-saas-mysql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: mysql
      port: {{ .Values.service.port }}
      targetPort: mysql
      protocol: TCP
  selector:
    {{- include "claw-saas-mysql.selectorLabels" . | nindent 4 }}
